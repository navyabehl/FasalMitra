streamlit>=1.32.0
pandas>=2.0.0
requests>=2.31.0
# anthropic is only needed if you pick "Anthropic" in AI explanation
# settings — Groq (the default) works with just `requests` above.
anthropic>=0.40.0
